package com.auctions.hunters.model.enums;

import com.auctions.hunters.model.Car;

/**
 * Ths enum document the types of category that a {@link Car} can have.
 */
public enum CategoryType {

    SUV,
    SEDAN,
    PICKUP,
    COUPE,
    CONVERTIBLE,
    ROADSTER,
    HATCHBACK,
    MINIVAN,
    CARGO_VAN,
}
